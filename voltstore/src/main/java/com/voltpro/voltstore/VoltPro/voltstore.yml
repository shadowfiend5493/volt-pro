info:
  name: voltstore
  type: http

http:
  method: GET
  url: http://localhost:8080/api/v1/voltstore
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
